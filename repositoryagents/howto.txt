SHORT VERSION?

LONG VERSION

1. INSTALL EIS

svn co https://apleis.svn.sourceforge.net/svnroot/apleis/trunk/apleis apleis 
got to the folder that contains the pom.xml
mvn install

2. INSTALL EISMASSIM

checkout <URL>
got to the folder that contains the pom.xml
mvn install

3. INSTALL APLTK

svn co https://apltk.svn.sourceforge.net/svnroot/apltk/trunk/apltk
got to the folder that contains the pom.xml
mvn install

4. PACKAGE JAVAAGENTS

checkout <URL>
got to the folder that contains the pom.xml
mvn package

5. COPY DEPENDENCIES

mvn dependency:copy-dependencies

6. RUN

java -jar target/javaagent-1.0.jar

7. ADAPTING CONFIGURATION FILES